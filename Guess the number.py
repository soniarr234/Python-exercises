"""
The program choose a number between 1 and 20 (you can use a fixed value initially).
The user tries to guess it, and the program indicates wheter the number is higher or lower until they guess correctly.
"""

import random

num_random = random.randint(1, 20)
print(num_random)
